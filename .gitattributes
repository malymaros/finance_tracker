# Normalize line endings on commit; let Git handle checkout conversion.
* text=auto

# Flutter-generated platform files are always written with LF by the toolchain.
# Pin them to LF so they never appear dirty after flutter pub get / flutter run.
linux/flutter/* text eol=lf
macos/Flutter/* text eol=lf
windows/flutter/* text eol=lf
